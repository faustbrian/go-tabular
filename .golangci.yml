version: "2"

linters:
  default: standard
  enable:
    - errorlint
    - exhaustive
    - gocritic
    - misspell
    - nilerr
    - revive
    - unconvert

formatters:
  enable:
    - gofmt
    - goimports
